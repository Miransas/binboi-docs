# Installation

Binboi CLI distribution is in progress. Prebuilt installers for the most common platforms are listed below.

> **Note:** Public distribution channels are being prepared. Until then, the CLI is available to early users on request — contact [contact@miransas.com](mailto:contact@miransas.com).

## npm

```bash
npm install -g @binboi/cli
```

_Coming soon._

## Homebrew (macOS / Linux)

```bash
brew install miransas/tap/binboi
```

_Coming soon._

## Cargo

```bash
cargo install binboi-cli
```

_Coming soon._

## pip

```bash
pip install binboi
```

_Coming soon._

## Curl installer

```bash
curl -fsSL https://binboi.com/install.sh | sh
```

_Coming soon._

## Verify

After installation, confirm the CLI is on your `PATH`:

```bash
binboi version
```

If the command is not found, add `/usr/local/bin` (or your platform's equivalent) to your `PATH`.

## Configuration

The CLI stores its config in `~/.binboi/config.json`. This includes your saved access token and any server overrides.

To reset:

```bash
rm -rf ~/.binboi
```

## Next

- [Authentication](/docs/authentication) — generate an access token.
- [Quick start](/docs/quick_start) — open your first tunnel.
- [CLI reference](/docs/cli) — all commands.